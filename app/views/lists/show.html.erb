<div class="container">
  <h1><%= @list.name %></h1>

  <div class="lists-bookmarks">

  <div class="card-product">
    <%@list.movies.each do |movie|  %>
      <%= image_tag movie.poster_url, alt: "Le Wagon", width: 200 %>
    <div class="card-product-infos">
      <%= movie.title %>
    <% bookmark = Bookmark.find_by(list: @list , movie: movie) %>
    <%= link_to "delete", bookmark_path(bookmark), data: { turbo_method: :delete, turbo_confirm: "Are you sure you "}%>
  <% end %>
  </div>
</div>

  </div>



<%= link_to "Add a film to bookmark", new_list_bookmark_path(@list) %>
<%= link_to "Go back to the list's list", root_path %>




























</div>
